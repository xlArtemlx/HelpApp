import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    dayItem: {
        paddingHorizontal: 17,
        alignItems: 'center'
    },
    dayNumber: {
        marginBottom: 5,
        fontSize: 12,
        fontWeight: 'bold'
    },
    dayName: {
        fontSize: 10
    }
})

import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    dayItem: {
        paddingHorizontal: 17,
        alignItems: 'center',
        margin: -3
    },
    dayNumber: {
        marginBottom: 5,
        fontSize: 12,
        fontWeight: 'bold'
    },
    dayName: {
        fontSize: 10
    }
})
